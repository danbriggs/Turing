int repeats (config&, int, int, int);
int repeatswithknown (config&, int, int, int);
bool fillsout (config&, int, int, int);
void compress (config&, compconfig&, int*, int);
void compress (config&, compconfig&);
void compress2 (config&, compconfig&);
bool conditionc (config&, int, int*);
bool conditionc (config&);